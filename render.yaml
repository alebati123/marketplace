services:
  - type: web
    name: marketplace-api
    env: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: ~18.0.0
      - key: PORT
        value: 10000
